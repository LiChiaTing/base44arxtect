import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { motion } from 'framer-motion';

const templates = [
  { name: "Academic Paper", description: "Standard template for research papers (IEEE, ACM, Nature)." },
  { name: "Resume/CV", description: "Professional and modern resume/CV layouts." },
  { name: "Presentation", description: "Sleek and professional Beamer presentation themes." },
  { name: "Thesis/Dissertation", description: "Comprehensive layouts for long academic documents." },
  { name: "Book/Report", description: "Structured templates for longer form writing." },
  { name: "Letter/Correspondence", description: "Formal letterheads and communication templates." },
];

export default function Template() {
  return (
    <motion.div 
      initial={{ opacity: 0 }}
      animate={{ opacity: 1 }}
      transition={{ duration: 0.5 }}
      className="container mx-auto py-16 px-6"
    >
      <div className="text-center">
        <h1 className="text-4xl md:text-5xl font-bold text-[#01102C] mb-6">Browse Our LaTeX Templates</h1>
        <p className="text-xl text-[#687B98] max-w-3xl mx-auto">
          Start your writing instantly with our curated collection of professional LaTeX templates, ready to use.
        </p>
      </div>

      <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-8 mt-12">
        {templates.map((template, index) => (
          <motion.div
            key={index}
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: index * 0.1 }}
          >
            <Card className="shadow-md hover:shadow-xl transition-all duration-300 h-full flex flex-col">
              <CardHeader>
                <CardTitle className="text-xl font-semibold text-[#1B4B91]">{template.name}</CardTitle>
              </CardHeader>
              <CardContent className="flex-grow flex flex-col justify-between">
                <p className="text-[#687B98] text-base mb-4">{template.description}</p>
                <Button variant="outline" className="mt-4 border-[#BA2377] text-[#BA2377] hover:bg-[#BA2377]/10 hover:text-[#BA2377]">
                  Preview & Use
                </Button>
              </CardContent>
            </Card>
          </motion.div>
        ))}
      </div>

      <p className="text-md text-slate-600 mt-16 text-center">
        Can't find what you're looking for? Use our <a href="#" className="text-[#1B4B91] font-semibold hover:underline">Template Converter</a> to import your own.
      </p>
    </motion.div>
  );
}